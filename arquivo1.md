# 📌 Guia Rápido de Git & GitHub

## 🚀 Primeira vez (repositório novo no GitHub)
```bash
git init                          # inicia o repositório local
git add .                         # adiciona os arquivos
git commit -m "primeiro commit"   # salva no histórico
git remote add origin https://github.com/SEUUSER/SEUREPO.git
git branch -M main                # garante que a branch principal seja main
git push -u origin main           # envia pro GitHub e cria o vínculo



git add .                          # adiciona tudo que mudou
git commit -m "mensagem clara"     # cria um commit
git push                           # envia pro GitHub




git pull     # atualiza o pc gom o github

[alterou arquivos] → git add . 
→ git commit -m "explica a mudança"
→ git push



Sempre use mensagens de commit claras (ex.: "ajuste tela de login", "corrigido bug no formulário").

git status mostra o que mudou.

git log --oneline mostra o histórico de commits.



